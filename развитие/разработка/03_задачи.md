# Задачи разработки

---

## TASK-001 — Базовая структура папок и пустые модули

**Приоритет:** 🔴 Критический  
**Оценка:** 2 ч

**Что сделать:**
1. Создать папку `src/preprocessing/` с пустыми модулями (только `__init__.py` и заглушки классов)
2. Создать папку `src/storage/` с `analysis_db.py` и `schema.sql`
3. Добавить зависимости в `requirements.txt`:
   - `scenedetect[opencv]>=0.6.3`
   - `opencv-python==4.8.1.78`
4. Написать скрипт `scripts/check_deps.py` — проверяет наличие всех зависимостей

**Критерии готовности:**
- `python3 -c "from src.preprocessing.pipeline import analyze_project"` — не падает
- `python3 -c "from src.storage.analysis_db import AnalysisDB"` — не падает

---

## TASK-002 — SQLite-обёртка (AnalysisDB)

**Приоритет:** 🔴 Критический  
**Оценка:** 3 ч

**Что сделать:**
Реализовать `src/storage/analysis_db.py`:
- `__init__(project_dir)` — создаёт `.videoeditor/analysis.db`
- `create_tables()` — применяет `schema.sql`
- `is_analyzed(path)` — проверяет `status='analyzed'` + валидность хэша
- `save_source_file(path)` → `source_files.id`
- `save_fragments(source_id, fragments)` — bulk INSERT
- `mark_error(path, error_msg)` — устанавливает `status='error'`
- `count_fragments(project_dir)` → int
- `reset_all(project_dir)` — DELETE + удаление превью

**Инвалидация кэша:**
```python
def _file_hash(path: str) -> str:
    with open(path, 'rb') as f:
        return hashlib.md5(f.read(65536)).hexdigest()
```
Сравнивать с сохранённым при каждом `is_analyzed()`.

**Критерии готовности:**
- 5 unit-тестов: create, save, read, cache-hit, cache-miss при изменении файла

---

## TASK-003 — SceneDetector

**Приоритет:** 🔴 Критический  
**Оценка:** 4 ч

**Что сделать:**
Реализовать `src/preprocessing/scene_detector.py`:

```python
from scenedetect import open_video, SceneManager, ContentDetector

class SceneDetector:
    def split(
        self, video_path: str,
        min_scene_length: float = 0.8,
        threshold: float = 27.0,
        frame_skip: int = 5,
    ) -> List[Scene]:
        video   = open_video(video_path)
        manager = SceneManager()
        manager.add_detector(
            ContentDetector(threshold=threshold, min_scene_len=int(min_scene_length * fps))
        )
        manager.detect_scenes(video, frame_skip=frame_skip)
        scene_list = manager.get_scene_list()
        return [
            Scene(start_s=s[0].get_seconds(), end_s=s[1].get_seconds())
            for s in scene_list
        ]
```

**Критерии готовности:**
- На тестовом видео 60 с находит 5+ сцен
- Минимальная длина сцены соблюдается
- Время работы < 30 с на 10 мин видео (с frame_skip=5)

---

## TASK-004 — QualityAnalyzer

**Приоритет:** 🔴 Критический  
**Оценка:** 6 ч

**Что сделать:**
Реализовать `src/preprocessing/quality_analyzer.py` — все метрики из `02_метрики_и_скоринг.md`.

Ключевые методы:
- `analyze(video_path, scene)` → `TechMetrics`
- `_sample_frames(video_path, start_s, end_s, n_frames=8)` → `List[np.ndarray]`
- `_compute_sharpness(frame)` → float
- `_compute_brightness(frame)` → float
- `_compute_stability(frames)` → float (optical flow)
- `_compute_motion(frames)` → float
- `_detect_defects(frames)` → dict

**Параметры выборки кадров:**
Равномерно, пропуская первый и последний кадр сцены (переходы).

**Критерии готовности:**
- На синтетически смазанном кадре sharpness < 0.05
- На чёрном кадре `is_black = True`
- На трясущемся видео stability < 0.25

---

## TASK-005 — ClipScorer

**Приоритет:** 🔴 Критический  
**Оценка:** 3 ч

**Что сделать:**
Реализовать `src/preprocessing/clip_scorer.py`:

```python
class ClipScorer:
    def score(self, m: TechMetrics, c: ContentMetrics = None) -> ScoreResult:
        quality_score   = compute_quality_score(m, c)
        style_scores    = compute_style_scores(m, c)
        return ScoreResult(quality_score=quality_score, **style_scores)
```

Формулы из `02_метрики_и_скоринг.md`.

**Критерии готовности:**
- Unit-тест: стабильный, резкий кадр → cinematic_score > 0.70
- Unit-тест: хаотичное движение → action_score > cinematic_score
- Unit-тест: is_black → quality_score < 0.20

---

## TASK-006 — PreviewGenerator

**Приоритет:** 🟡 Важный  
**Оценка:** 2 ч

**Что сделать:**
Реализовать `src/preprocessing/preview_generator.py`:

```python
def generate_thumbnail(
    source_path: str, start_s: float, end_s: float,
    output_path: str, size: str = '320x180',
) -> str:
    mid_s = (start_s + end_s) / 2.0
    cmd = [
        'ffmpeg', '-y', '-ss', str(mid_s), '-i', source_path,
        '-vframes', '1', '-s', size, output_path,
    ]
    subprocess.run(cmd, check=True, capture_output=True)
    return output_path
```

**Критерии готовности:**
- Thumbnail создаётся для каждого фрагмента
- При отсутствии ffmpeg — чёткое сообщение об ошибке
- Путь сохраняется в БД

---

## TASK-007 — Pipeline (оркестрация)

**Приоритет:** 🔴 Критический  
**Оценка:** 4 ч

**Что сделать:**
Реализовать `src/preprocessing/pipeline.py` — функцию `analyze_project()` из `03_техническая_интеграция.md`.

**Особенности:**
- `ProcessPoolExecutor(max_workers=cpu_count // 2)` — без multiprocessing.Pool
- Инкрементальность: пропускать уже проанализированные файлы
- `progress_callback(filename, done, total)` → вызывается после каждого файла
- Обработка исключений: `db.mark_error()` + логирование, не роняет всю очередь

**Критерии готовности:**
- Повторный запуск на той же папке: время < 2 с
- При ошибке одного файла остальные обрабатываются
- progress_callback вызывается ровно N раз (N = количество pending файлов)

---

## TASK-008 — FragmentLibrary

**Приоритет:** 🔴 Критический  
**Оценка:** 5 ч

**Что сделать:**
Реализовать `src/preprocessing/clip_library.py` — полный API из `03_техническая_интеграция.md`.

Ключевой метод — `get_candidates()`:
- SQL: `SELECT * FROM fragments WHERE source_id = ? AND quality_score >= ? AND duration_s BETWEEN ? AND ? ORDER BY quality_score DESC LIMIT ?`
- Конвертация `FragmentRow → CandidateClip` через `_fragment_to_candidate()`

**Критерии готовности:**
- `get_candidates()` возвращает список, совместимый с `CandidateClipBuilder.build_candidates()`
- `has_analyzed()` возвращает False для нового файла и True после анализа
- `get_fragments_for_ui()` возвращает thumbnail_path для каждого фрагмента

---

## TASK-009 — Интеграция в gui.py

**Приоритет:** 🟡 Важный  
**Оценка:** 3 ч

**Что сделать:**
Добавить в `src/gui.py`:

1. Кнопку «Анализировать материалы» с прогресс-баром (до выбора стиля)
2. Блок статуса кэша: «✅ проанализировано · N фрагментов · X ч назад»
3. В логике рендера: ветку `if lib.has_analyzed(video_path)` → читать кандидатов из БД

**Требования:**
- «Пропустить анализ» → fallback на существующий пайплайн (ничего не ломать)
- Нет принудительного анализа при каждом рендере

**Критерии готовности:**
- Кнопка появляется только если выбрана папка проекта с видеофайлами
- При нажатии прогресс-бар показывает ход анализа
- Рендер без анализа работает идентично текущему поведению

---

## TASK-010 — Страница просмотра фрагментов

**Приоритет:** 🟡 Важный  
**Оценка:** 4 ч

**Что сделать:**
Добавить в GUI страницу/секцию «Фрагменты» (согласно `02_интерфейс.md`):

- Сетка 4 колонки с thumbnails
- Заголовок карточки: filename + временной диапазон + звёздный рейтинг
- Фильтр по минимальному quality_score (слайдер 0.0–1.0)
- Кнопки: одобрить / отключить / избранный (вызов `lib.mark_approved()`, `lib.mark_priority()`)
- По умолчанию: top-50 фрагментов, min_quality=0.55

**Критерии готовности:**
- Показывает реальные thumbnails (не плейсхолдеры)
- Одобренные/отключённые фрагменты сохраняются в БД и учитываются при следующем рендере

---

## TASK-011 — ContentAnalyzer (MediaPipe) — Уровень 2

**Приоритет:** 🟢 Важный  
**Оценка:** 6 ч

**Что сделать:**
Реализовать `src/preprocessing/content_analyzer.py` согласно `02_продвинутая_версия.md`.

Зависимости:
```
mediapipe>=0.10.0
```

**Критерии готовности:**
- На фото с лицом: `has_face=True`, `face_area_ratio` > 0
- На фото без лица: `has_face=False`
- Время анализа одного кадра < 0.2 с

---

## TASK-012 — DuplicateDetector — Уровень 2

**Приоритет:** 🟢 Нейтральный  
**Оценка:** 4 ч

**Что сделать:**
Реализовать `src/preprocessing/duplicate_detector.py`.

```
imagehash>=4.3.1
Pillow>=10.0.0
```

Логика:
1. После анализа всех сцен → сгруппировать по расстоянию Хэмминга < 8
2. В каждой группе → оставить с наибольшим quality_score
3. Остальные → `is_duplicate=True` в таблице fragments

**Критерии готовности:**
- Два кадра из одного кадрового источника → обнаруживаются как дублики
- Кардинально разные кадры → расстояние > 12

---

## TASK-013 — Тесты производительности

**Приоритет:** 🔴 Критический  
**Оценка:** 3 ч

**Что сделать:**
Написать скрипт `scripts/benchmark_preprocessing.py`:

```python
# Тест 1: время полного анализа
start = time.time()
n = analyze_project(TEST_PROJECT_DIR)
elapsed = time.time() - start
assert elapsed < 120.0, f"Too slow: {elapsed:.1f}s"
assert n >= 10, f"Too few fragments: {n}"

# Тест 2: повторный запуск
start = time.time()
analyze_project(TEST_PROJECT_DIR)
elapsed = time.time() - start
assert elapsed < 2.0, f"Cache not working: {elapsed:.1f}s"

# Тест 3: качество кандидатов
lib = FragmentLibrary(AnalysisDB(TEST_PROJECT_DIR))
candidates = lib.get_candidates(min_quality=0.55)
avg_q = sum(c.features.technical_quality_score for c in candidates) / len(candidates)
assert avg_q > 0.55, f"Low quality: {avg_q:.2f}"
```

**Критерии готовности:**
- Все 3 теста проходят на тестовой папке с 10+ видеофайлами
