# Спецификация: локальный анализ видео на MacBook Pro M3 Pro (32 ГБ)

**Дата:** 2026-07-01
**Цель:** выбрать оптимальную локальную мультимодальную модель и инструмент запуска для анализа видео (описание сцен, объектов, эмоций, композиции) в рамках проекта автоматического видеомонтажа.
**Железо (фактически измерено):** MacBook Pro, Apple M3 Pro — 12 CPU (6P+6E), **18-core GPU** (старшая версия), **36 ГБ** unified memory, bandwidth ~150 ГБ/с.
**⚠️ Реальное ограничение — диск:** свободно ~30 ГБ; в `~/.lmstudio/models` уже лежит 50 ГБ моделей (GLM-4.7-Flash 23 ГБ, gpt-oss-20b 11 ГБ, phi-4 8.4 ГБ — текстовые; **gemma-3-12b-it-qat-4bit 7.5 ГБ — vision, готова к тесту**).

---

## 1. Ключевые выводы ресерча

### 1.1. Видео напрямую или через кадры?

**Главный практический вывод: локально видео анализируется через извлечение кадров.**

- Архитектурно современные VLM (Qwen3-VL, MiniCPM-V 4.5, Gemma 4) *умеют* понимать видео — но эта возможность полноценно работает только в оригинальных рантаймах (transformers/vLLM на CUDA).
- **LM Studio и его OpenAI-совместимый API принимают только изображения** (JPEG/PNG/WebP). Прямой подачи видеофайла нет.
- llama.cpp (на котором работают GGUF-модели в LM Studio и Ollama) обрабатывает видео как последовательность картинок.
- Поэтому пайплайн один: **ffmpeg/PySceneDetect → кадры → пакетная подача в модель → JSON**. Это не ограничение, а норма для локального стека, и для задачи монтажа это даже удобнее: мы сами контролируем, какие кадры анализировать.

### 1.2. Память и квантизация на 32 ГБ

- macOS + фоновые приложения съедают ~8–10 ГБ. Комфортный бюджет под модель + контекст: **до ~16 ГБ**, максимум ~20 ГБ (дальше свопинг и деградация всей системы).
- **Q4_K_M — рекомендуемый дефолт**: лучший баланс качество/размер, потеря качества минимальна. Q5_K_M — если есть запас памяти. Q6/Q8 на 32 ГБ имеет смысл только для моделей ≤8B. Ниже Q4 (Q3, Q2) — заметная деградация именно визуального анализа, не рекомендуется.
- На Mac предпочтительнее **MLX-версии моделей** (в LM Studio они помечены как MLX): нативное ускорение Apple Silicon, на 20–40% быстрее GGUF при том же качестве.

Ориентировочная занимаемая память (вес модели Q4 + vision-энкодер + контекст):

| Размер модели | RAM фактически | Вердикт для M3 Pro 32 ГБ |
|---|---|---|
| 2–4B | 3–5 ГБ | Свободно, ноутбук не замечает |
| 7–9B | 6–9 ГБ | **Оптимум** — комфортная работа |
| 12–14B | 10–13 ГБ | Хорошо, но параллельно тяжёлое уже не запустишь |
| 27–32B dense | 18–22 ГБ | На грани; медленно (prefill картинок долгий), вентиляторы |
| 30B-A3B (MoE) | ~18 ГБ | Память большая, но **скорость как у 3B** — интересный вариант |

### 1.3. Скорость на M3 Pro (реалистичные ожидания)

M3 Pro заметно медленнее M3 Max (150 против 300–400 ГБ/с bandwidth). Реалистично:

- 8B Q4/MLX: **~25–40 ток/с** генерация; обработка одного изображения (prefill vision-энкодера) — 2–5 сек.
- 12B: ~15–25 ток/с.
- 27–32B dense: ~8–12 ток/с + долгий prefill — анализ сотен кадров становится мучительным.
- Практика: **1 кадр с развёрнутым описанием ≈ 10–25 сек** на 8B-модели. 100 кадров ≈ 20–40 минут. Это и есть главное ограничение локального анализа — планировать количество кадров надо экономно.

### 1.4. Ограничения контекста

- Один кадр в высоком разрешении = 500–1500 токенов (зависит от модели; у MiniCPM-V 4.5 есть 3D-Resampler со сжатием 96×).
- В один запрос реально помещать **1–4 кадра**; больше — падает качество внимания к деталям и растёт память под KV-кэш.
- Для монтажа лучше **1 кадр = 1 запрос** (стабильный JSON), с опцией «2–3 соседних кадра» для оценки движения.

---

## 2. Сравнительная таблица моделей

| Модель | В LM Studio | Изображения | Видео через кадры | Размер (Q4) | Реком. квант. | Нагрузка на M3 Pro 32 ГБ | Качество визуального анализа | Простота запуска | API | Плюсы | Минусы | Рекомендация |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **Qwen3-VL 8B Instruct** | ✅ (GGUF + MLX) | ✅ | ✅ | ~5–6 ГБ | Q4_K_M / MLX 4bit | Низкая–средняя | ★★★★★ лучший в классе ≤10B: объекты, текст, пространственные отношения, композиция | Очень просто | ✅ OpenAI-совм. | SOTA для своего размера; отличный OCR; хорошо следует JSON-схеме; MLX-версия быстрая | Русские описания чуть суше английских | 🥇 **Основной выбор для теста** |
| **Qwen3-VL 4B** | ✅ | ✅ | ✅ | ~3 ГБ | Q4_K_M / Q6 | Минимальная | ★★★★ удивительно хорош для 4B | Очень просто | ✅ | Быстрый (кадр за ~5–10 сек); почти не греет ноутбук; можно гнать тысячи кадров | Меньше деталей в описании эмоций/эстетики | 🥈 **Лёгкий вариант** для массового прогона кадров |
| **MiniCPM-V 4.5 (8B)** | ✅ (GGUF) | ✅ | ✅ (лучшая видео-архитектура: 3D-Resampler, 96× сжатие кадров) | ~5–6 ГБ | Q4_K_M | Низкая–средняя | ★★★★★ топ OpenCompass среди <30B; заточен под видео | Просто (также в Ollama) | ✅ | Специализация именно на видео/high-FPS; эффективные токены на кадр; можно подавать серии кадров дёшево | Полное видео-API раскрывается в родном рантайме, не в LM Studio; сообщество меньше | 🥇 **Альтернатива основному** — стоит протестировать вторым |
| **Qwen3-VL 30B-A3B (MoE)** | ✅ | ✅ | ✅ | ~18 ГБ | Q4_K_M | Средняя по памяти, **низкая по compute** (активно 3B) | ★★★★★ качество ~уровня 32B при скорости 4B | Просто | ✅ | Лучшее качество, которое реально быстро работает на M3 Pro | Съедает 18+ ГБ — почти всё закрыть; долгая загрузка | 🥉 **Качественный тяжёлый вариант** |
| **Gemma 3 12B** | ✅ (GGUF + MLX) | ✅ | ✅ | ~8 ГБ | Q4_K_M | Средняя | ★★★★ хорошие естественные описания сцен, эмоций, эстетики | Очень просто | ✅ | Красивый «человеческий» язык описаний; хорош в русском | Слабее Qwen3-VL в OCR и мелких деталях; 896×896 фикс. разрешение | Хороший запасной вариант |
| **Gemma 4 (~12B класс)** | ✅ | ✅ | ✅ (заявлено нативное видео, но в LM Studio — всё равно кадры) | ~8–9 ГБ | Q4_K_M / MLX | Средняя | ★★★★☆ | Просто | ✅ | Свежая архитектура, 256K контекст, thinking-режим | GGUF-поддержка новее = менее обкатана; видео-режим локально не даёт преимуществ | Присмотреться после MVP |
| **GLM-4.6V-Flash 9B** | ✅ | ✅ | ✅ | ~6 ГБ | Q4_K_M | Низкая–средняя | ★★★★ | Просто | ✅ | Оптимизирован под локальный запуск | Меньше комьюнити/примеров; русский слабее | Опционально |
| **Qwen2.5-VL 7B** | ✅ | ✅ | ✅ | ~5 ГБ | Q4_K_M | Низкая | ★★★☆ | Очень просто | ✅ | Проверенный, много гайдов | Устарел — Qwen3-VL лучше во всём при том же размере | Брать только если Qwen3-VL глючит |
| **Mistral Small 3.x 24B (vision)** | ✅ | ✅ | ✅ | ~14 ГБ | Q4_K_M | Средняя–высокая | ★★★★ | Просто | ✅ | Сильная общая эрудиция | Медленнее и тяжелее Qwen3-VL 8B без явного выигрыша в vision | Не приоритет |
| **Qwen3-VL 32B / Gemma 4 31B** | ✅ | ✅ | ✅ | ~19–20 ГБ | Q4_K_M | **Высокая**: 8–12 ток/с, нагрев, всё остальное закрыть | ★★★★★ | Просто, но некомфортно | ✅ | Максимальное качество локально | На M3 Pro 32 ГБ — на пределе; анализ библиотеки видео займёт часы | Не рекомендуется на этом железе (MoE 30B-A3B лучше) |
| **LLaVA 1.6 (7B/13B)** | ✅ | ✅ | ✅ | 4–8 ГБ | Q4_K_M | Низкая | ★★☆ | Просто | ✅ | Классика | Морально устарел (2024) | ❌ Не брать |

---

## 3. Сравнение инструментов запуска

| Инструмент | Тип | Vision-поддержка | API | Простота на Mac | Вердикт |
|---|---|---|---|---|---|
| **LM Studio** | GUI-приложение | ✅ отличная (GGUF + **MLX-движок**, drag&drop картинок) | ✅ OpenAI-совместимый локальный сервер (`localhost:1234/v1`), structured output (JSON schema) | ★★★★★ скачал → выбрал модель → работает | 🥇 **Выбор для MVP.** Единственный GUI-инструмент с нативным MLX = максимальная скорость на Apple Silicon |
| **Ollama** | CLI + сервис | ✅ хорошая (qwen3-vl, minicpm-v4.5, gemma3 и др.) | ✅ свой API + OpenAI-совместимый | ★★★★☆ `brew install ollama; ollama run qwen3-vl` | 🥈 Лучший вариант для продакшн-скриптов без GUI; поддержка новейших vision-моделей иногда отстаёт от llama.cpp |
| **llama.cpp (llama-server)** | CLI | ✅ (mtmd, первым получает новые модели) | ✅ OpenAI-совместимый | ★★★☆ нужна компиляция/brew, флаги | Для продвинутой фазы: максимум контроля, минимум удобства |
| **MLX / mlx-vlm** | Python-библиотека | ✅ нативный Apple Silicon, у mlx-vlm есть утилиты для видео-кадров | Через код | ★★★☆ `pip install mlx-vlm` | Отличен для встраивания в Python-пайплайн на фазе 2; самый быстрый рантайм на Mac |
| **Jan** | GUI | ✅ базовая | ✅ | ★★★★ | Симпатично, но vision-возможности беднее LM Studio |
| **Open WebUI** | Веб-фронтенд | ✅ (через бэкенд Ollama/llama.cpp) | Проксирует | ★★★ нужен Docker + бэкенд | Это UI, а не рантайм — лишний слой для нашей задачи |
| **AnythingLLM** | GUI (RAG-фокус) | Частично | ✅ | ★★★ | Заточен под документы/RAG, не под видеоанализ |
| **Pinokio** | Установщик AI-приложений | Зависит от приложения | Зависит | ★★★ | Непредсказуемость окружений; не для стабильного пайплайна |

**Вывод по инструментам:** LM Studio закрывает оба приоритета — простейшая установка *и* локальный OpenAI-совместимый API, который позже подключается к пайплайну монтажа одной строкой (`base_url="http://localhost:1234/v1"`). Ollama — план Б, когда GUI станет не нужен.

---

## 4. Практическая рекомендация по моделям

| Роль | Модель | Квантизация | Когда использовать |
|---|---|---|---|
| **Первый тест (основная)** | **Qwen3-VL 8B Instruct** | MLX 4bit (или Q4_K_M GGUF) | Стартовая точка. Лучший баланс качество/скорость/память в 2026. ~6 ГБ RAM, кадр за 10–20 сек |
| **Лёгкая** | **Qwen3-VL 4B** | Q4_K_M / Q6 | Массовый прогон: тысячи кадров, черновая разметка библиотеки, работа от батареи |
| **Качественная (тяжёлая)** | **Qwen3-VL 30B-A3B (MoE)** | Q4_K_M | Финальный отбор лучших сцен, когда нужна максимальная точность. Запускать с закрытыми остальными приложениями |
| **Дополнительно протестировать** | MiniCPM-V 4.5 | Q4_K_M | Единственный кандидат, архитектурно заточенный под видео; сравнить с Qwen3-VL 8B на своих роликах |

Практический совет: **двухпроходная схема** — 4B быстро размечает все кадры (грубые теги: люди/природа/движение/брак), затем 8B или 30B-A3B детально описывает только кадры-кандидаты. Это в разы сокращает общее время.

---

## 5. Архитектура MVP: локальный видеоанализ

```
┌─────────────┐   ┌──────────────────┐   ┌─────────────────────┐   ┌───────────────┐
│ Видеофайлы   │ → │ Извлечение кадров  │ → │ VLM через LM Studio │ → │ JSON-результаты │
│ (путь юзера) │   │ ffmpeg / PySceneDetect │  │ API :1234/v1        │   │ + скоринг сцен  │
└─────────────┘   └──────────────────┘   └─────────────────────┘   └───────────────┘
                                                                          ↓
                                                              Автовыбор сцен под сценарий монтажа
```

### 5.1. Компоненты

1. **Вход:** пользователь указывает путь к папке/файлам (mp4, mov).
2. **Извлечение кадров** (Python):
   - `PySceneDetect` (`detect-adaptive`) → границы сцен → 1–3 ключевых кадра на сцену; 
   - fallback: `ffmpeg -vf "fps=1/3"` (кадр каждые 3 сек) или `select='gt(scene,0.3)'`;
   - даунскейл до ~1024px по длинной стороне (качество анализа не падает, prefill в 2–3 раза быстрее).
3. **Анализ** — запросы к LM Studio (OpenAI-совместимый API, base64-изображения), **structured output по JSON-схеме**:
   ```json
   {
     "scene_description": "…", "objects": ["…"], "people": {"count": 0, "emotions": ["…"]},
     "composition": "…", "motion_estimate": "static|slow|fast",
     "lighting": "…", "aesthetic_score": 0.0, "usable_for_edit": true, "tags": ["…"]
   }
   ```
4. **Оценка движения:** пары соседних кадров одним запросом («что изменилось между кадрами?») либо дёшево через optical flow (OpenCV) без LLM.
5. **Хранение:** JSON на видео (`analysis/{video_id}.json`): метаданные, таймкоды кадров, описания, скоры.
6. **Выбор сцен:** отдельный модуль сопоставляет сценарий монтажа с тегами/скорами и отдаёт EDL-список (видео, таймкод входа/выхода, причина выбора).

### 5.2. Параметры по умолчанию

| Параметр | Значение | Обоснование |
|---|---|---|
| Кадров на сцену | 1–2 | Баланс скорость/полнота |
| Разрешение кадра | ≤1024px | Быстрый prefill без потери качества |
| Кадров в запросе | 1 (2–3 для движения) | Стабильный JSON |
| Контекст модели | 8K | Хватает на кадр + схему + ответ; экономит RAM |
| temperature | 0.1–0.3 | Детерминированность разметки |
| Параллелизм | 1 запрос (LM Studio 0.4.13+ умеет parallel predictions — можно 2) | Не душить память |

### 5.3. Ограничения, которые нужно принять

- **Скорость** — главный лимит: ~150–400 кадров/час на 8B. Значит: анализируем сцены, а не «каждую секунду».
- Движение между далёкими кадрами модель оценивает грубо — точную динамику брать из optical flow.
- Длинный связный «сюжет всего ролика» лучше собирать вторым проходом: текстовые описания сцен → обычная LLM суммаризирует.

---

## 6. Итоговый вывод: стек для MVP

| Приоритет | Решение |
|---|---|
| 1. Простая установка | **LM Studio** (одно приложение, GUI + сервер) |
| 2. Умеренная нагрузка | **Qwen3-VL 8B, MLX 4bit** (~6 ГБ RAM, ноутбук остаётся рабочим) |
| 3. Качество анализа | Qwen3-VL 8B — SOTA ≤10B; апгрейд-путь до 30B-A3B без смены стека |
| 4. API на будущее | Уже есть: OpenAI-совместимый `http://localhost:1234/v1` + JSON schema |
| 5. Пригодность для автомонтажа | Пайплайн ffmpeg/PySceneDetect → LM Studio API → JSON → скоринг встраивается в существующий проект video editor напрямую |

**Первые шаги (скорректированы под фактическую систему — LM Studio, ffmpeg, Python 3.11 уже установлены):**
1. **Тест прямо сейчас, без скачивания:** `gemma-3-12b-it-qat-4bit` уже лежит в LM Studio — она vision-capable. Кинуть в чат 5–10 кадров из своих видео, оценить качество описаний.
2. Скачать **`Qwen3-VL-8B-Instruct` MLX 4bit (~5 ГБ)** — основная модель MVP. Сравнить с Gemma 3 12B на тех же кадрах: Qwen точнее в деталях/OCR и быстрее, Gemma — «человечнее» описания.
3. Опционально: `Qwen3-VL-4B` (~3 ГБ) для массового прогона.
4. Поднять сервер LM Studio, написать скрипт: PySceneDetect → кадры → API → JSON.
5. **30B-A3B пока отложить** — при 30 ГБ свободного диска скачивание 18 ГБ впритык. Если понадобится: сначала удалить неиспользуемые текстовые модели (GLM-4.7-Flash 23 ГБ / gpt-oss-20b 11 ГБ / phi-4 8.4 ГБ освободят место). С 36 ГБ RAM модель пойдёт комфортно.

---

## Источники

- [LM Studio Model Catalog](https://lmstudio.ai/models)
- [LM Studio: unified multi-modal MLX engine](https://lmstudio.ai/blog/unified-mlx-engine)
- [LM Studio: Image Input API](https://lmstudio.ai/docs/typescript/llm-prediction/image-input)
- [Best Local Vision-Language Models for LM Studio & Ollama (2026)](https://lmsa.app/blog/best-local-vision-language-models-for-lm-studio-ollama-march-2026-update/)
- [Best Vision Models You Can Run Locally | InsiderLLM](https://insiderllm.com/guides/vision-models-locally/)
- [Best Qwen Models Ranked | InsiderLLM](https://insiderllm.com/guides/qwen-models-guide/)
- [Mac for Local LLMs: Apple Silicon Guide (2026)](https://llmpicker.blog/posts/mac-for-local-llms/)
- [MiniCPM-V GitHub (OpenBMB)](https://github.com/OpenBMB/MiniCPM-V)
- [MiniCPM-V 4.5 · Hugging Face](https://huggingface.co/openbmb/MiniCPM-V-4_5)
- [minicpm-v4.5 · Ollama](https://ollama.com/library/minicpm-v4.5)
- [llama.cpp Apple Silicon performance discussion](https://github.com/ggml-org/llama.cpp/discussions/4167)
- [Qwen llama.cpp docs](https://qwen.readthedocs.io/en/latest/run_locally/llama.cpp.html)
