# План разработки: Auto Video Editor V2

**Основание:** [TZ_AUTO_VIDEO_EDITOR_V2.md](TZ_AUTO_VIDEO_EDITOR_V2.md) + [SPEC_LOCAL_VIDEO_ANALYSIS.md](SPEC_LOCAL_VIDEO_ANALYSIS.md)
**LLM-стек (зафиксировано):** LM Studio (API `localhost:1234/v1`) + **Qwen3-VL 8B Instruct MLX 4bit** — визуальный анализ ключевых кадров сцен и ранжирование сцен под сценарий. Резерв: Gemma 3 12B QAT (уже скачана). Лёгкий прогон: Qwen3-VL 4B (опционально).

## Принципы (ТЗ §24)

1. Три сущности в центре: **Проект → Сцена → Монтажный план**. Всё остальное — сервисы вокруг них.
2. Никакого «монтажа сразу после загрузки»: сначала карта видео.
3. LLM — ранжировщик и описатель, а не «монтажёр»: OpenCV/ffmpeg/PySceneDetect дают факты, Qwen3-VL — смысл (ТЗ §8).
4. Монтажный план — самостоятельная, сериализуемая сущность с причинами выбора.
5. Слои: core (анализ/план/рендер, без UI) ← cli ← Streamlit UI.

## Архитектура

```
Video_editorai/
├── config.py            # пути, LM Studio URL, модель, параметры анализа
├── cli.py               # analyze / plan / render из терминала
├── app.py               # Streamlit UI (Фазы 4, 8)
├── core/
│   ├── models.py        # Project, SourceVideo, Scene, MontagePlan, PlanSegment
│   ├── storage.py       # SQLite (проекты, видео, сцены, планы, история замен)
│   ├── media_import.py  # файлы/папка → список исходников
│   ├── video_analyzer.py# ffprobe: техметаданные
│   ├── scene_detector.py# PySceneDetect: границы сцен
│   ├── frame_quality.py # OpenCV: резкость/экспозиция/тряска/движение
│   ├── previews.py      # thumbnails + preview clips (ffmpeg)
│   ├── llm_analyzer.py  # Qwen3-VL 8B через LM Studio: описания/теги/тип сцены
│   ├── pipeline.py      # оркестрация анализа → каталог сцен
│   ├── audio_analyzer.py# librosa: BPM, биты, энергетика (Фаза 5)
│   ├── presets.py       # 12 пресетов (Фаза 6)
│   ├── montage_planner.py # слоты драматургии, подбор, план (Фаза 6)
│   └── renderer.py      # preview/final render ffmpeg (Фаза 7)
└── projects/            # данные проектов: db + previews/thumbnails/render/cache
```

## Фазы (= задачи в трекере)

| # | Фаза | Содержание | Статус |
|---|---|---|---|
| 1 | **Фаза 0** | Каркас: модели, SQLite, конфиг | в работе |
| 2 | **Фаза 1** | Анализ исходников: импорт, ffprobe, PySceneDetect, качество кадров, превью | — |
| 3 | **Фаза 2** | LLM-анализ сцен (Qwen3-VL 8B, JSON schema, 1 кадр = 1 запрос, даунскейл 1024px) | — |
| 4 | **Фаза 3** | Пайплайн + CLI: папка с видео → каталог сцен в SQLite | — |
| 5 | **Фаза 4** | Streamlit: экран каталога сцен с реальными превью | — |
| 6 | **Фаза 5** | Анализ музыки: BPM, биты, энергетика, точки склеек | — |
| 7 | **Фаза 6** | Пресеты + Matcher + Montage Planner (LLM-ранжирование карточек) | — |
| 8 | **Фаза 7** | Preview render + финальный экспорт (9:16 / 16:9 / 1:1) | — |
| 9 | **Фаза 8** | Таймлайн + замена фрагментов + пересборка | — |

MVP (ТЗ §21) закрывается фазами 0–8. Вне MVP — по ТЗ §22.

## Ключевые решения

- **UI: Streamlit** — знаком по текущему проекту; обязательно `fileWatcherType=none` (известный конфликт с torch). Тяжёлый анализ — вне UI-процесса (CLI/subprocess), чтобы UI не зависал.
- **Двухпроходный LLM-анализ** (из спеки): при больших библиотеках — сначала быстрая разметка, детальные описания только для кандидатов. В MVP — один проход Qwen3-VL 8B.
- **Graceful degradation:** если LM Studio не запущен — пайплайн завершает технический анализ и качество, LLM-поля помечаются `pending_llm`, дозаполняются отдельной командой.
- **JSON-схема сцены** — единый контракт между llm_analyzer, каталогом и планировщиком (ТЗ §9).
- **Причина выбора** хранится у каждого сегмента плана — прозрачность логики (ТЗ §2, §23).
